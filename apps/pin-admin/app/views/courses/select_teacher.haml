=form_for @course,:url=>"/courses/#{@course.id}/set_teacher",:method=>:put do |f|
  %div
    =f.select(:teacher_id,Teacher.all.map{|teacher|[teacher.real_name,teacher.id]})
    =link_to "新建教师","/teachers/new"
  %div
    =f.submit "确定"
		
