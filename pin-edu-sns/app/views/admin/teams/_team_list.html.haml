.attrs
  .t2 名称
  .t2 班级序号
  .t2 教师

- teams.each do |team|
  .model.team
    .t2= team.name
    .t2= team.cid
    .t2
      .t2
        - if team.teacher.blank?
          %span.c 未指定
        - else
          .ava= avatar User.new, :mini
          %span.c= team.teacher.real_name

      .ops
        %a.remove{:href=>'javascript:;'} 删除
        %a.detail{:href=>"/admin/teams/#{team.id}"} 查看详情