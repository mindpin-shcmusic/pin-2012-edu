- if logged_in?
  :javascript
    window.USER_INFO = {
      id : #{current_user.id},
      name : #{current_user.name.to_json},
      email : #{current_user.email.to_json},
      channels : {

      },
      paths : {

      }
    }