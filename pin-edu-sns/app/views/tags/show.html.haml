- if params[:action] == 'show'
  .link
    = link_to "我的资源","/tags/#{params[:tag_name]}/mine"
  .link
    = link_to "公共资源","/tags/#{params[:tag_name]}/public"
  .link
    = link_to "其他人分享的资源","/tags/#{params[:tag_name]}/shared"
- else
  .page-media-resource-list
    - @media_resources.each do |media_resource|
      .media-resource
        = media_resource.path
  